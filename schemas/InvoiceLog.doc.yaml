    /**
        * @swagger
        * components:
        *   schemas:
        *     InvoiceLog:
        *       type: object
        *       description: 'Представление записи о счете, как она хранится в логах базы данных.'
        *       properties:
        *         id:
        *           type: integer
        *           description: 'Уникальный идентификатор записи в логе.'
        *           example: 202
        *         created_at:
        *           type: string
        *           format: date-time
        *           description: 'Дата и время создания записи.'
        *           # Кавычки здесь ОБЯЗАТЕЛЬНЫ, так как строка содержит ':'
        *           example: "2024-08-31T12:35:00.123Z"
        *         email:
        *           type: string
        *           format: email
        *           description: 'Email клиента.'
        *           example: 'client@example.com'
        *         works:
        *           type: string
        *           description: "Строка, содержащая массив объектов 'Work' в формате JSON".
        *           # Кавычки здесь ОБЯЗАТЕЛЬНЫ, так как строка содержит '[', '{', '"'
        *           example: '[{"name":"Разработка логотипа","cost":15000},{"name":"Верстка","cost":25000}]'
        *       required:
        *         - id
        *         - created_at
        *         - email
        *         - works
      *